# Faça um programa para calcular a área de um retângulo.

largura = float(input(f'Digite a largura do retângulo:'))
comprimento = float(input(f'Digite o comprimento do retângulo:'))

print(f'a área do seu retângulo é : {largura * comprimento:.1f}')

